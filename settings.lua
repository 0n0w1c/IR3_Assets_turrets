if mods["space-age"] then
    data:extend({
        {
            type = "bool-setting",
            name = "IR3-reskin-rocket-turret",
            setting_type = "startup",
            default_value = true,
            order = "a",
        }
    })
end
